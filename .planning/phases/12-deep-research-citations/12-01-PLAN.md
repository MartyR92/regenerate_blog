---
phase: 12-deep-research-citations
plan: 01
type: execute
wave: 1
depends_on: ["11-01", "11-02"]
files_modified:
  - scripts/research.py
  - context/agent-prompts/writer-v1.md
autonomous: true
requirements:
  - RES-01
  - RES-02
  - RES-03
must_haves:
  truths:
    - "Research data contains URLs, authors, and source titles"
    - "Writer Agent integrates citations naturally into markdown"
  artifacts:
    - path: "scripts/research.py"
      provides: "Enhanced search with Interactions API integration"
    - path: "context/agent-prompts/writer-v1.md"
      provides: "Citation-aware writing instructions"
---

<objective>
Enhance the Research Agent to use the Interactions API for deep citations and update the Writer Agent prompt to integrate these citations into the blog posts.

Purpose: Provides factual grounding and transparency by surfacing verifiable sources directly in the articles.
Output: Updated research script and writer prompt.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-deep-research-citations/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Research Script for Interactions API Citations</name>
  <files>scripts/research.py</files>
  <action>
    Modify `scripts/research.py` to utilize the `INTERACTIONS_API_KEY`.
    When calling the search APIs, extract not just the summary but also the metadata: `source_url`, `author`, `publication_date`, and `title`.
    Format the research queue output (`context/research-queue.json`) to include a `citations` array for each research item.
    Ensure each citation contains the required metadata for the Writer Agent.
  </action>
  <verify>grep "INTERACTIONS_API_KEY" scripts/research.py && grep "citations" scripts/research.py</verify>
  <done>Research agent extracts and structures citation metadata from search results.</done>
</task>

<task type="auto">
  <name>Task 2: Update Writer Agent Prompt for Citation Integration</name>
  <files>context/agent-prompts/writer-v1.md</files>
  <action>
    Modify `context/agent-prompts/writer-v1.md`.
    Add a new section: "Citation & Fact-Checking Protocol".
    Instruct the agent to use the `citations` array from the research data.
    Require the use of Markdown footnotes `[^1]` or inline links for all factual claims.
    Instruct the agent to create a "References" or "Sources" section at the end of each post, listing all cited works with full metadata.
    Enforce the [VERIFY] tag only for claims that *cannot* be backed by the provided citations.
  </action>
  <verify>grep "Citation & Fact-Checking Protocol" context/agent-prompts/writer-v1.md && grep "footnotes" context/agent-prompts/writer-v1.md</verify>
  <done>Writer agent is instructed to use citations and maintain a bibliography for each post.</done>
</task>

</tasks>

<verification>
Check research queue output for structured citation metadata and verify the writer prompt includes explicit citation instructions.
</verification>

<success_criteria>
Articles generated by the pipeline contain verifiable citations and a references section.
</success_criteria>

<output>
After completion, create `.planning/phases/12-deep-research-citations/12-01-SUMMARY.md`
</output>
