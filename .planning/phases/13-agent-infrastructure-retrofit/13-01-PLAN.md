---
phase: 13-agent-infrastructure-retrofit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/visual.py
autonomous: true
requirements:
  - RET-01
must_haves:
  truths:
    - "visual.py can process multiple files in a single run"
    - "Agent skips files that already have a hero image"
  artifacts:
    - path: "scripts/visual.py"
      provides: "Bulk processing capability for Unsplash hero images"
---

<objective>
Update visual.py to support processing all existing articles in the content directories to facilitate the Milestone v5.0 retrofit.

Purpose: Automates the enrichment of the entire blog archive with high-quality hero images.
Output: An enhanced visual.py script with bulk processing support.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-agent-infrastructure-retrofit/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Bulk Processing Logic in visual.py</name>
  <files>scripts/visual.py</files>
  <action>
    Modify `scripts/visual.py`.
    Replace the single-file selection logic with a function that gathers all `.md` files from `content/de/posts/` and `content/en/posts/`.
    Iterate through the gathered files.
    For each file, check if `featureImage:` is already present in the front matter. Skip if it exists.
    Include a `time.sleep(2)` between Unsplash/Gemini calls to avoid rate limits.
  </action>
  <verify>grep "content/de/posts" scripts/visual.py && grep "time.sleep" scripts/visual.py</verify>
  <done>visual.py is capable of iterating through the entire content archive and applying hero images to missing files.</done>
</task>

</tasks>

<verification>
Run `python scripts/visual.py --help` (if CLI args implemented) or verify logic via code review.
</verification>

<success_criteria>
Script correctly identifies all 6 existing articles and identifies which ones lack hero images.
</success_criteria>

<output>
After completion, create `.planning/phases/13-agent-infrastructure-retrofit/13-01-SUMMARY.md`
</output>
