---
phase: 13-agent-infrastructure-retrofit
plan: 02
type: execute
wave: 1
depends_on: ["13-01"]
files_modified:
  - scripts/image.py
autonomous: true
requirements:
  - RET-02
  - RET-03
must_haves:
  truths:
    - "image.py generates both a chart and a diagram per run"
    - "Visuals are placed in distinct sections of the article"
  artifacts:
    - path: "scripts/image.py"
      provides: "Multi-visual generation with specific type targeting (Chart/Diagram)"
---

<objective>
Update image.py to generate a specific mix of visuals (1 chart + 1 diagram) per article and implement smart placement logic.

Purpose: Ensures every article has a diverse and contextually relevant set of technical visuals.
Output: An enhanced image.py script with multi-visual support and type targeting.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-agent-infrastructure-retrofit/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Multi-Visual Logic and Type Targeting in image.py</name>
  <files>scripts/image.py</files>
  <action>
    Modify `scripts/image.py`.
    Update `process_file` to perform two separate generation cycles:
    - Cycle 1: Target "Technical Chart" (Data focus, e.g., trends, comparisons).
    - Cycle 2: Target "Technical Diagram" (System focus, e.g., flows, structures).
    Update prompts to explicitly distinguish between these types.
    Change output filenames to `technical_visual_2_{lang}.svg` and `technical_visual_3_{lang}.svg` (since technical_diagram_1 exists).
    Ensure WebP fallbacks are generated for both.
  </action>
  <verify>grep "technical_visual_2" scripts/image.py && grep "Technical Chart" scripts/image.py</verify>
  <done>image.py can generate distinct visual types and save them with unique filenames.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Smart Placement Logic</name>
  <files>scripts/image.py</files>
  <action>
    Modify `scripts/image.py` placement logic.
    Instead of just checking for the first `##`, implement logic to find all `##` headers.
    Place Visual 2 after the second `##` header (if available).
    Place Visual 3 after the third `##` header (if available).
    If headers are insufficient, append to the end or after different paragraphs.
  </action>
  <verify>grep "split('##')" scripts/image.py</verify>
  <done>Visuals are distributed through the article content rather than clumped together.</done>
</task>

</tasks>

<verification>
Check script logic for loop control and placement index handling.
</verification>

<success_criteria>
Script produces two new visual files with WebP fallbacks and inserts them into separate sections of the markdown.
</success_criteria>

<output>
After completion, create `.planning/phases/13-agent-infrastructure-retrofit/13-02-SUMMARY.md`
</output>
