---
phase: 11-visual-infrastructure-rendering
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - layouts/_default/_markup/render-image.html
autonomous: false
requirements:
  - VIS-01
must_haves:
  truths:
    - "Images render properly using a <picture> tag with WebP fallback"
    - "SVG aspect ratios are preserved on mobile"
    - "Paths dynamically resolve correctly for Hugo builds"
  artifacts:
    - path: "layouts/_default/_markup/render-image.html"
      provides: "Custom image rendering hook"
  key_links:
    - from: "layouts/_default/_markup/render-image.html"
      to: "Markdown Image Output"
      via: "Hugo Build"
      pattern: "<picture>"
---

<objective>
Implement a custom Hugo render hook to ensure robust browser rendering of diagrams via SVG with a WebP fallback.

Purpose: Fulfills the requirement to flawlessly render diagrams on `renatureforce.com/blog` by utilizing `<picture>` tags and preserving responsive behavior without relying on Blowfish's default image optimization.
Output: A custom markdown render hook file.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Custom Picture Render Hook</name>
  <files>layouts/_default/_markup/render-image.html</files>
  <action>
    Create `layouts/_default/_markup/render-image.html` (create directories if needed) to override the theme default.
    Implement logic: Use `{{ $src := .Destination }}`. Check if it ends in `.svg`.
    If it is an SVG, replace the `.svg` extension with `.webp` to form the fallback source.
    Output a `<picture>` block containing `<source srcset="{{ $base }}.webp" type="image/webp">` and `<img src="{{ $src }}" alt="{{ .Text }}" loading="lazy" style="max-width: 100%; height: auto;">`.
    If not an SVG, output a standard `<img>` tag with the source.
    Include `<figcaption>{{ .Title | markdownify }}</figcaption>` if `.Title` exists.
    Ensure paths are correctly rendered verbatim since `image.py` now outputs the explicit root-relative path (`/blog/images/...`).
  </action>
  <verify>test -f layouts/_default/_markup/render-image.html && grep "<picture>" layouts/_default/_markup/render-image.html</verify>
  <done>Markdown references to SVGs generate valid HTML picture tags with both WebP and SVG sources, preserving mobile aspect ratio.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Hugo Image Render Hook Override</what-built>
  <how-to-verify>
    1. Run `hugo server --baseURL="/blog/"`.
    2. Open a test draft containing an image reference.
    3. Verify in browser devtools that the `<picture>` element is present with correct `/blog/images/...` paths for both WebP and SVG.
    4. Resize the window to ensure the SVG scales while preserving its aspect ratio.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Check Hugo build logs to ensure the render hook does not produce errors and the output HTML correctly references both file formats.
</verification>

<success_criteria>
All markdown images render as `<picture>` tags with functioning paths.
</success_criteria>

<output>
After completion, create `.planning/phases/11-visual-infrastructure-rendering/11-02-SUMMARY.md`
</output>