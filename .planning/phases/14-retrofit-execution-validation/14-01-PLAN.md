---
phase: 14-retrofit-execution-validation
plan: 01
type: execute
wave: 1
depends_on: ["13-01", "13-02"]
files_modified:
  - content/de/posts/*.md
  - content/en/posts/*.md
  - static/images/2026/**
autonomous: true
requirements:
  - VIS-03
  - VIS-04
  - VIS-05
  - VIS-06
  - RET-04
  - RET-05
must_haves:
  truths:
    - "Every article has a hero image"
    - "Every article has 3 technical visuals (existing + 2 new)"
    - "All visuals render correctly with WebP fallbacks"
  artifacts:
    - path: "static/images/2026/"
      provides: "Full set of retrofit visual assets"
---

<objective>
Execute the visual retrofit on all 6 existing articles using the upgraded agents and verify the results.

Purpose: Finalizes Milestone v5.0 and ensures the blog archive meets the new high-quality visual standard.
Output: Retrofitted markdown files and a complete set of visual assets.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute visual-agent Retrofit</name>
  <files>scripts/visual.py</files>
  <action>
    Run `python scripts/visual.py`.
    Verify that it identifies and processes all articles lacking a hero image.
    Confirm Unsplash images are downloaded and front matter is updated.
  </action>
  <verify>grep "featureImage" content/de/posts/*.md && grep "featureImage" content/en/posts/*.md</verify>
  <done>All articles have hero images assigned.</done>
</task>

<task type="auto">
  <name>Task 2: Execute image-agent Retrofit</name>
  <files>scripts/image.py</files>
  <action>
    Run `python scripts/image.py`.
    Verify that it generates two additional visuals (chart + diagram) for every article.
    Confirm SVG and WebP files are created in the correct directories.
    Confirm markdown bodies are updated with the new visuals.
  </action>
  <verify>ls static/images/2026/*/technical_visual_2_*.svg && ls static/images/2026/*/technical_visual_3_*.svg</verify>
  <done>All articles have a complete set of 3 technical visuals.</done>
</task>

<task type="auto">
  <name>Task 3: Validation and Verification</name>
  <files>public/**</files>
  <action>
    Run `hugo` to build the site locally.
    Inspect the output for any broken image links.
    Verify that the `<picture>` tag logic is working for the new visuals.
  </action>
  <verify>hugo --gc --minify</verify>
  <done>The retrofit is verified and the site builds without errors.</done>
</task>

</tasks>

<verification>
Manual spot check of 2-3 articles in the built site to confirm visual quality and placement.
</verification>

<success_criteria>
6 articles x 1 hero = 6 new hero images.
6 articles x 2 new visuals = 12 new technical visuals (with WebP fallbacks).
All markdown files updated correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/14-retrofit-execution-validation/14-01-SUMMARY.md`
</output>
